pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'tia-tutorial'

dependencyResolutionManagement {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}